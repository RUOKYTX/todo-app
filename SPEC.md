# Spec：任务标签 + 标签筛选 + 标签记忆（含截止日期回归基线）

> 由 to-spec 会话共识整理。下方 "附录 A" 为历史 spec（任务截止日期 + 顶部统计，已实现），作为本次回归基线保留。

## Problem Statement

用户使用「今日待办」管理任务，但任务只有文字与截止时间两个维度，没有任何分类手段。任务一多，工作/生活/学习混在一起：想"只看工作"必须从头翻整个列表；每次新建任务重复输入相同标签，成本高、易出错。

## Solution

1. 每条任务可添加**多个标签**（如：工作、生活、学习），随时增删，随账号持久化、多端实时同步。
2. 任务列表上方有**标签筛选栏**：点某个标签只显示带该标签的任务，点"全部"恢复；筛选时统计数字跟随筛选结果。
3. **标签记忆**：曾经用过的标签被记住，添加标签时输入框弹出候选（历史 + 当前任务中已有标签），点击即用，按账号隔离。

## User Stories

1. 作为用户，我希望每条任务可以打多个标签（工作、生活、学习…），以便用分类维度组织任务。
2. 作为用户，我希望添加任务时就能顺手填标签（逗号分隔），以便创建时一步到位。
3. 作为用户，我希望任务创建后也能随时增删标签，以便分类需求变化时灵活调整。
4. 作为用户，我希望标签以简洁的小胶囊展示在任务行内，以便一眼看到任务归属。
5. 作为用户，我希望点标签胶囊上的 × 即可删除该标签，以便快速移除。
6. 作为用户，我希望任务行内有"＋标签"入口，点击变行内输入框、回车确认，以便不离开列表即可加标签。
7. 作为用户，我希望行内加标签支持 Esc 取消、失焦确认，以便与现有"双击编辑"交互范式一致。
8. 作为用户，我希望已完成的任务不允许增删标签，以便与"已完成不可编辑"的既有规则一致。
9. 作为用户，我希望重复标签自动去重、空白/非法输入被忽略、标签长度受限，以便数据保持干净。
10. 作为用户，我希望列表上方有标签筛选栏，点某个标签只显示带该标签的任务，以便快速聚焦某一类任务。
11. 作为用户，我希望点"全部"立即恢复完整列表，以便随时回到全量视图。
12. 作为用户，我希望筛选状态下的统计（顶部"未完成 x / 共 y"与底部 counter）跟随筛选结果，以便数字与所见列表一致。
13. 作为用户，我希望筛选后无结果时有明确提示（而非空白），以便知道当前筛选无匹配任务。
14. 作为用户，我希望当前筛选的标签被删光后自动回到"全部"，以便不会停留在无意义的空筛选状态。
15. 作为用户，我希望筛选栏的标签按使用频率排序，以便最常用的标签在最前面。
16. 作为用户，我希望输入过的标签被记住，下次添加时输入框弹出候选、点击即填，以便不再重复手打。
17. 作为用户，我希望标签记忆按账号隔离，以便切换账号后互不干扰。
18. 作为用户，我希望标签数据随账号持久化、多端实时同步，以便换设备也不丢、多窗口一致。
19. 作为用户，我希望标签增删与其他操作（勾选、编辑、删除、改期）互不冲突，现有功能完全不受影响。
20. 作为用户，我希望新增标签后能立即通过实时同步在其他窗口看到，以便多端协作一致。
21. 作为用户，我希望历史任务（无标签字段/脏数据）能正常渲染为无标签状态，以便老数据不崩溃。

## Implementation Decisions

### 数据层

- `tags` 列已存在：`text`，存 JSON 数组字符串（如 `["工作","生活"]`）。
- 前端任务对象增加 `tags: string[]`；`parseTags(raw)` 纯函数容错解析：`null`/空串/非法 JSON → `[]`，非字符串元素过滤。此函数必须能安全处理历史脏数据（User Story 21）。
- 写入：标签增删时对整数组 `JSON.stringify` 后写 `tags` 列（单列全量更新，不做 JSON 局部 patch）。
- `parseTagInput(str)` 纯函数：按中英文逗号拆分 → trim → 去空 → 去重，单标签 ≤12 字符、每任务 ≤5 个（超出部分丢弃）。

### 筛选

- 筛选状态 `activeTag: string | null`（`null` = 全部）。
- 派生列表 `getVisibleTasks()`：`activeTag ? tasks.filter(t => t.tags.includes(activeTag)) : tasks`。
- 渲染与统计（列表、顶部统计、底部 counter、空状态提示）统一基于派生列表——**统计跟随筛选**，现有统计函数调用点零改动。
- 筛选栏标签集合动态收集自全部任务，按**使用频次降序**排列（同频次按首现顺序）；"全部"始终在最前。
- 若 `activeTag` 已不存在于任何任务（标签被删光/任务被删），自动回落"全部"。
- 筛选空结果时提示文案："该标签下暂无任务"（区别于全空列表的"暂无任务，先添加一条吧"）。

### 标签记忆

- 存储：`localStorage`，key `tag_memory_<userId>`（按账号隔离，登出无需清理）。
- 写入时机：任何成功添加标签的操作（表单提交、行内添加）；去重后按最近使用排序，**上限 30 个**（超出丢弃最旧）。
- 候选来源：历史标签 ∪ 当前任务中出现过的标签，合并去重，排除已在输入框中输入的标签。
- UI：自定义轻量下拉面板（白底圆角 + 阴影，紫主色 hover），同一组件复用绑定两处输入框（添加表单标签框、行内＋标签框）；聚焦/输入时弹出，点击填入，支持方向键 + 回车选择，失焦/选择后隐藏。

### 交互

- 添加表单改为两行布局：第一行 = 任务文字输入 + 添加按钮（元素与行为不变）；第二行 = 截止时间输入 + 标签输入（选填，逗号分隔）。提交时标签与任务一并写入。
- 任务行改为两行：第一行 = 勾选框 + 文字 + 截止徽章 + 删除按钮（原样）；第二行 = 标签胶囊区（每枚胶囊带内嵌 ×）+ "＋标签"按钮。
- 标签增删走乐观更新 + 后台写库 + 失败回滚对齐（与现有 `updateTask` 完全同构）。
- 已完成任务：标签区域交互禁用（与"已完成不可编辑文字/改期"一致）。
- 事件委托：列表 click 新增两分支——点击胶囊内 × 删除标签；点击"＋标签"进入行内输入。

### 不改动

认证、实时订阅、勾选、双击编辑文字、双击改期、删除、头像菜单、弹窗、toast、30 秒逾期轮询、counter 文案结构——全部保持原样。

## Testing Decisions

- **Seam（单一 seam）**：浏览器 E2E（`@playwright/test`）。纯函数断言通过 `page.evaluate` 在页面上下文直接调用（页面为全局作用域函数），**不引入第二套单元测试框架**，不引入模块化/构建链。
- **好测试的标准**：只断言外部可观察行为——DOM 呈现、localStorage、数据库持久化（刷新后仍在）、跨窗口实时同步、筛选结果与统计数字；不测内部实现细节。
- **测试账号**：UI 全流程自动注册随机邮箱（`test_<时间戳>@example.com`，密码 ≥8 位）——Supabase 邮箱验证已确认关闭；teardown 通过页面内 `supabaseClient.rpc('delete_user')` 清理账号，不污染真实数据。
- **多端场景**：数据隔离用双 browser context（各自独立 localStorage）注册两个账号互查；实时同步用同 context 双 page（共享登录态）互查。
- **网络故障回滚**：用 Playwright route 拦截使写库请求失败，断言乐观更新回滚。
- **逾期标红**：改库（页面内 supabaseClient 将 due_date 改为过去时间）后 `page.evaluate` 触发 `refreshDueMarks()`，断言红色徽章出现；避免等真实 30 秒轮询。
- **用例矩阵**：现有功能回归 R1–R11 + 标签新功能 F1–F14（见下方验证清单）。
- **测试基建不进 Git 仓库**：`tests/`、`playwright.config.js`、`package.json`、`server.mjs`、`node_modules` 全部 `.gitignore`（本地回归工具，非交付物）。
- **先例**：项目历史上无自动化测试（SPEC 附录 A 为手动回归）；本 spec 是首个自动化测试基线，覆盖全部旧 R 用例作为回归护栏。

### 验证用例：标签新功能（F）

| # | 前置 | 操作 | 期望 |
|---|------|------|------|
| F1 | 已登录 | 表单填标签"工作，生活"（中文逗号）添加任务 | 任务行显示"工作""生活"两枚标签胶囊 |
| F2 | 已登录 | 添加不带标签的任务 | 任务行无标签胶囊、无"＋标签"以外的标签元素 |
| F3 | 任务带 2 个标签 | 点某胶囊的 × | 该标签消失且落库（刷新仍在）；删光后标签区仅剩"＋标签" |
| F4 | 已登录 | 点"＋标签"输入"学习"回车 | 胶囊"学习"出现并落库；Esc 取消不产生脏数据 |
| F5 | 已登录 | 重复添加同一标签、输入空串/仅逗号、超 12 字、第 6 个标签 | 去重；空输入忽略；超长截断；第 6 个被丢弃（纯函数 `parseTagInput` 断言） |
| F6 | 已完成任务 | 点其 × 与"＋标签" | 无反应（不可增删） |
| F7 | 3 条任务（工作×2、生活×1） | 点筛选栏"工作" | 只显示 2 条带"工作"的任务；点"全部"恢复 3 条 |
| F8 | 筛选"工作" | 查看顶部与底部统计 | 数字 = 筛选后 2 条的统计，非全量 |
| F9 | 筛选"生活"（该标签下仅 1 条） | 删除该任务 | 显示"该标签下暂无任务"且筛选自动回落"全部" |
| F10 | 添加过标签"学习" | 新建任务聚焦标签输入框 | 弹出候选含"学习"；点击候选填入输入框 |
| F11 | 账号 A 用过标签"工作" | 新 context 注册账号 B | B 的候选列表无 A 的历史（localStorage key 按 userId 隔离） |
| F12 | 任务带标签 | 刷新页面 | 标签保留；筛选栏含该标签 |
| F13 | 双窗口同账号 | 窗口 A 给任务加标签 | 窗口 B 标签实时出现；窗口 A 删除标签 B 同步消失 |
| F14 | 数据库 tags 为 null / 非法 JSON / 混入非字符串 | 刷新加载 | 任务正常渲染为无标签（`parseTags` 断言） |

### 验证用例：现有功能回归（R，自动化基线）

| # | 验证点 | 期望 |
|---|--------|------|
| R1 | 未登录访问 | 显示登录页，看不到任务区 |
| R2 | 注册 / 登录 / 登出；错误密码、短密码 | 全流程正常，错误提示友好 |
| R3 | 添加、勾选/取消、删除、双击编辑文字（回车保存/Esc 取消） | 全部照常工作 |
| R4 | 数据隔离 | 账号 A 看不到账号 B 的任务 |
| R5 | 实时同步 | 另一窗口的增删改自动出现 |
| R6 | 刷新后数据不丢 | 任务与登录态保持 |
| R7 | 480px 视口布局 | 新增元素不挤压/遮挡现有元素，无横向溢出 |
| R8 | counter 文案 | `共 X 项，已完成 Y 项，未完成 Z 项` 不变 |
| R9 | 截止日期：添加带/不带时间、徽章文案（今天/明天/月日）、双击改期、Esc 取消 | 与附录 A 一致 |
| R10 | 逾期标红（改库为过去时间 + 触发刷新） | 未完成逾期任务徽章红 + "已过期"；勾选完成后红色消失 |
| R11 | 顶部统计与操作联动 | `未完成 x / 共 y` 每一步实时正确 |
| R12 | 网络故障（route 拦截失败） | 添加/勾选/删除/改期/改标签均乐观回滚且数据对齐 |

## Out of Scope

- 标签管理页（重命名、合并、批量打标）
- 标签颜色/图标自定义
- 按标签排序或分组展示
- 移动端专项适配
- 第二套单元测试框架 / 模块化改造（单一 seam 原则）
- 修改 `tags` 列本身（已存在）
- CI 流水线接入（本地 `npm test` 即可，gh 未登录）

## Further Notes

- 样式沿用现有设计语言：主色 `#6366f1`、危险红 `#ef4444`、圆角 8–12px、浅紫底 `#eef2ff`。
- 记忆上限 30 个标签防止 localStorage 膨胀；key 含 userId 天然账号隔离。
- 行内标签编辑复用现有"双击编辑"的交互范式（回车/Esc/失焦），不引入新弹窗。
- 发布说明：无 issue tracker（gh 未登录），spec 以仓库文件保存；测试基建按用户要求不进 Git。

---

# 附录 A：任务截止日期 + 顶部统计（历史 spec，已实现，作为回归基线）

> 原 spec 全文保留。本次标签功能不得破坏以下任何行为。

## Problem Statement

用户使用「今日待办」管理任务，但任务没有时间约束：无法知道每件事何时必须完成，也无法快速看到还有哪些没做完。任务一多，列表变成一锅粥——看不到哪些快到期、哪些已经逾期。

## Solution

1. 每条任务可设置**选填截止日期**（精确到时分），未完成且已过期的任务在列表里以**红色徽章 + "已过期"标记**醒目提示；已完成任务即使过期也不标红。
2. 界面顶部（日期行右侧）常驻显示统计 **`未完成 x / 共 y`**，无任务时隐藏。

## User Stories

1. 作为用户，我希望添加任务时可选择是否设置截止时间，以便仅对有时限的任务做规划。
2. 作为用户，我希望截止时间精确到时分，以便表达"今天下午 2 点半前"这种精确需求。
3. 作为用户，我希望无截止时间的任务不显示任何日期元素，以免列表被空徽章污染。
4. 作为用户，我希望未完成且已过期的任务显示红色日期徽章和"已过期"提示，以便一眼识别最紧急的任务。
5. 作为用户，我希望完成一个已过期的任务后红色标记消失，以便视觉上反馈"已处理"。
6. 作为用户，我希望日期徽章显示为"今天 14:30 / 明天 14:30 / 7月31日 14:30"这类人性化格式（跨年带年份），以便不用换算就能读懂。
7. 作为用户，我希望双击日期徽章即可修改截止时间（回车保存、Esc 取消、失焦保存），以便任务顺延时快速改期。
8. 作为用户，我希望页面挂着不动时，任务到点后 30 秒内自动变红，以便不刷新也能看到逾期变化。
9. 作为用户，我希望顶部常驻显示"未完成 x / 共 y"，以便随时知道剩余工作量。
10. 作为用户，我希望任务列表为空时顶部统计隐藏，以便界面干净。
11. 作为用户，我希望顶部统计在我增删改勾选任务后立即更新，以便数字始终准确。
12. 作为用户，我希望所有现有功能（登录注册、勾选、双击编辑、删除、实时同步、数据隔离）完全不受影响，以便平滑升级。
13. 作为用户，我希望截止时间数据随账号持久化、多设备实时同步，以便换设备也不丢。

## Implementation Decisions

### 数据层

- `due_date` 列已存在，类型 `timestamptz`；Supabase JS 返回 ISO 字符串，前端 `new Date()` 解析（本地时区显示）。
- `fetchTasks` 映射结果增加 `due_date` 字段（保留 `id/text/done` 结构不变）。
- 新增任务的 insert 语句增加 `due_date`，值为 `datetime-local` 输入（`YYYY-MM-DDTHH:mm`），未填写时存 `null`。

### 逾期判定（纯函数，可单测）

- `isOverdue(task)`：`!task.done && task.due_date !== null && new Date(task.due_date) < Date.now()`
- 边界：截止时刻未到不算逾期；已完成任务永不过期；无截止日期永不过期。

### 显示格式（纯函数，可单测）

- `formatDue(task)`：今天→`今天 HH:mm`；明天→`明天 HH:mm`；同年→`M月D日 HH:mm`；跨年→`YYYY年M月D日 HH:mm`。

### 交互

- 添加区：任务输入框右侧新增 `<input type="datetime-local">`（选填，回车/＋与任务一并提交）。
- 编辑：双击徽章 → 原地替换为 `datetime-local` 输入框，回车保存 / Esc 取消 / 失焦保存；保存走乐观更新 + 后台写库 + 失败回滚（与现有 `updateTask` 完全同构）。
- 渲染：`render()` 中每个任务生成徽章，逾期加 `.overdue` 类；勾选完成/取消时随重渲染自动更新。
- 顶部统计：`#date-text` 所在行改为 flex 布局，右侧新增统计 span；`updateHeaderStats()` 输出 `未完成 x / 共 y`，`tasks.length === 0` 时隐藏。
- 实时性：登录后启动 30 秒 `setInterval`，每次检查所有徽章的 overdue 状态与文案，有变化才重渲染（不发网络请求）；登出时清除定时器。

### 不改动

现有 counter（`共 X 项，已完成 Y 项…`）、认证、实时订阅、编辑文字、删除、数据隔离逻辑——全部保持原样。

## Testing Decisions

- **Seam**：项目为原生 JS + Supabase CDN、无构建工具、无测试框架，**不引入新 seam**（新增依赖违背"现有功能一个都别动"）。
- **验证方式**：两层——(a) 纯函数 `isOverdue` / `formatDue` 的边界用例在浏览器 Console 手工断言；(b) 全量浏览器手动回归清单（下方）。
- **好测试的标准**：只验证外部可观察行为（徽章是否红、统计数字是否对、数据是否落库），不验证内部实现细节。

### 验证用例：新功能（F）

| # | 前置 | 操作 | 期望 |
|---|------|------|------|
| F1 | 已登录 | 添加任务时填截止时间（如明晚 20:00） | 任务带徽章 `明天 20:00` |
| F2 | 已登录 | 添加任务不填截止时间 | 任务无任何日期元素 |
| F3 | 任务截止今天 14:00，现在 14:00 整 | 查看徽章 | 未过期（严格 `<`，整点不算过期） |
| F4 | 任务截止今天 14:00，现在 14:00:01 | 查看徽章 | 徽章红色 + 已过期标记 |
| F5 | 有逾期任务 | 勾选完成该任务 | 红色消失（灰化徽章），不标过期 |
| F6 | 已逾期任务 | 取消勾选 | 红色恢复 |
| F7 | 有徽章任务 | 双击徽章改期（回车保存） | 日期更新，落库后刷新仍在 |
| F8 | 编辑徽章 | 按 Esc / 点别处 | 取消 / 失焦保存，不产生脏数据 |
| F9 | 页面挂着不动 | 等任务到点 | 30 秒内自动变红，无需任何操作 |
| F10 | 修改时网络故障 | 保存改期 | 乐观回滚 + 数据对齐 |
| F11 | 无任务 | 查看顶部 | 统计隐藏 |
| F12 | 3 条任务 2 条未完成 | 查看顶部 | 显示 `未完成 2 / 共 3` |
| F13 | 操作列表 | 依次添加/勾选/删除/编辑 | 顶部数字每一步实时正确 |
| F14 | 截止时间跨年（2026-12-30 创建的 2027-01-02） | 查看徽章 | 带年份 `2027年1月2日` |
| F15 | 今天/明天边界（23:50 截止今天、次日 00:10 截止） | 查看徽章 | 分别显示 `今天 23:50` / `明天 00:10` |
| F16 | 双窗口同时在线 | 窗口 A 添加带截止日期的任务 | 窗口 B 30 秒内（实时推送）出现带徽章 |
| F17 | 刷新页面 | 刷新 | 徽章、截止时间、统计全部保留 |

### 验证用例：现有功能回归（R）

| # | 验证点 | 期望 |
|---|--------|------|
| R1 | 未登录访问 | 显示登录页，看不到任务区 |
| R2 | 登录 / 注册 | 正常，错误提示友好 |
| R3 | 添加、勾选/取消、删除、双击编辑文字 | 全部照常工作 |
| R4 | 数据隔离 | 账号 A 看不到账号 B 的任务 |
| R5 | 实时同步 | 另一窗口的改动自动出现 |
| R6 | 刷新后数据不丢 | 任务与登录态保持 |
| R7 | 界面文字清晰、布局不乱 | 新增元素不挤压/遮挡现有布局（480px 内） |
| R8 | 现有 counter 文案 | `共 X 项，已完成 Y 项，未完成 Z 项` 不变 |

## Out of Scope

- 截止日期的排序/分组展示（如"按到期时间排序"）
- 到期提醒（通知/邮件）
- 自动化测试框架引入（Playwright 等）
- 修改 `due_date` 列本身（已存在）
- 移动端适配专项

## Further Notes

- 所有新增 CSS 沿用现有变量风格（主色 #6366f1、危险红 #ef4444、圆角 8–12px）。
- 徽章编辑复用现有"双击文字编辑"的交互范式，不引入新弹窗。
- 30 秒定时器是**纯前端视觉刷新**，不产生网络请求、不触碰 Supabase 配额。
- 发布说明：`gh` 未登录，本 spec 以仓库文件形式保存；如需发布到 GitHub Issues，请先 `gh auth login`。
